        let list = "";
        list += "<table><tr><th>증감연산자</th><th>설명</th></tr>";
        list += "<tr><td>변수++</td><td>기존의 변수 값에 1을 더합니다(후위)</td>";
        list += "</tr><tr><td>++변수</td><td>기존의 변수 값에 1을 더합니다(전위)</td>";
        list += "</tr><tr><td>변수--</td><td>기존의 변수 값에 1을 뺍니다(후위)</td>";
        list += "</tr><tr><td>--변수</td><td>기존의 변수 값에 1을 뺍니다(전위)</td></tr></table>";
        document.write(list);